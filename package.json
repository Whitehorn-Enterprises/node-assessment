{
  "name": "node-assessment",
  "version": "1.0.0",
  "description": "1. Run `npm install` to install testing dependencies.\r 2. Create a file called `server.js`. All your server code and endpoints should be in this file.\r 3. Create a file called `userCtrl.js`. You will write your endpoints and export each one from this file.\r ** NOTE: `module.exports` your `app` at the end of `server.js`, otherwise the tests won't work **",
  "main": "users.js",
  "directories": {
    "test": "test"
  },
  "ava": {
    "files": [
      "test/*.js"
    ]
  },
  "scripts": {
    "test": "ava --verbose",
    "start": "node server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ava": "^0.18.1",
    "body-parser": "^1.16.0",
    "immutable": "^3.8.1",
    "lodash": "^4.17.4",
    "sinon": "^1.17.7",
    "supertest": "^3.0.0",
    "supertest-as-promised": "^4.0.2"
  },
  "repository": {
    "url": "git+https://github.com/DevMountain/node-assessment.git"
  },
  "bugs": {
    "url": "https://github.com/DevMountain/node-assessment/issues"
  },
  "homepage": "https://github.com/DevMountain/node-assessment#readme",
  "devDependencies": {}
}
